[
  {
    "value": "Delivered email from {email.from.address} to {email.to.address}",
    "conditions": [{ "field": "event.dataset", "value": "delivery" }]
  },
  {
    "value": "Protected {email.to.address} by {event.action} URL {url.original}",
    "conditions": [
      { "field": "event.dataset", "value": "url protect" },
      { "field": "event.action" },
      { "field": "email.to.address" },
      { "field": "url.original" }
    ]
  },
  {
    "value": "TTP URL {mimecast.ttp.url.action} on {url.original} for {email.to.address}",
    "conditions": [
      { "field": "event.dataset", "value": "ttp_url" },
      { "field": "mimecast.ttp.url.action" },
      { "field": "url.original" },
      { "field": "email.to.address" }
    ]
  },
  {
    "value": "Attachment scan result {mimecast.ttp.attachment.result} on file {file.name} from {email.from.address}",
    "conditions": [
      { "field": "event.dataset", "value": "ttp_attachment" },
      { "field": "mimecast.ttp.attachment.result" },
      { "field": "file.name" },
      { "field": "email.from.address" }
    ]
  },
  {
    "value": "Impersonation attempt {mimecast.ttp.impersonation.action} from {email.from.address} to {email.to.address}",
    "conditions": [
      { "field": "event.dataset", "value": "ttp_impersonation" },
      { "field": "mimecast.ttp.impersonation.action" },
      { "field": "email.from.address" },
      { "field": "email.to.address" }
    ]
  },
  {
    "value": "DLP policy {mimecast.dlp.policy} triggered: {mimecast.dlp.action} on email from {email.from.address}",
    "conditions": [
      { "field": "event.dataset", "value": "dlp" },
      { "field": "mimecast.dlp.policy" },
      { "field": "mimecast.dlp.action" },
      { "field": "email.from.address" }
    ]
  },
  {
    "value": "Audit event {mimecast.audit.event_type} by {source.user.email}",
    "conditions": [
      { "field": "event.dataset", "value": "audit" },
      { "field": "mimecast.audit.event_type" },
      { "field": "source.user.email" }
    ]
  },
  {
    "value": "Threat {mimecast.threat.type} detected: {mimecast.threat.action} on email from {email.from.address}",
    "conditions": [
      { "field": "event.dataset", "value": "threat" },
      { "field": "mimecast.threat.type" },
      { "field": "mimecast.threat.action" },
      { "field": "email.from.address" }
    ]
  },
  {
    "value": "{mimecast.siem.rejection.info} for email from {email.from.address}",
    "conditions": [
      { "field": "mimecast.siem.rejection.info" },
      { "field": "email.from.address" }
    ]
  },
  {
    "value": "{event.action} {email.direction} email from {email.from.address} to {email.to.address}",
    "conditions": [
      { "field": "event.action" },
      { "field": "email.direction" },
      { "field": "email.from.address" },
      { "field": "email.to.address" }
    ]
  },
  {
    "value": "Spam detected in email from {email.from.address} to {email.to.address}",
    "conditions": [
      { "field": "event.dataset", "value": "spam" },
      { "field": "email.from.address" },
      { "field": "email.to.address" }
    ]
  },
  {
    "value": "AV detected {mimecast.siem.virus_found} in file {file.name} from {email.from.address}",
    "conditions": [
      { "field": "event.dataset", "value": "av" },
      { "field": "mimecast.siem.virus_found" },
      { "field": "file.name" },
      { "field": "email.from.address" }
    ]
  },
  {
    "value": "Attachment Protect triggered on file {file.name}",
    "conditions": [
      { "field": "event.dataset", "value": "attachment protect" },
      { "field": "file.name" }
    ]
  },
  {
    "value": "{event.action} {email.direction} email from {source.ip} to {email.to.address}",
    "conditions": [
      { "field": "event.action" },
      { "field": "email.direction" },
      { "field": "source.ip" },
      { "field": "email.to.address" }
    ]
  }
]
