{
  "arguments": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Arguments",
    "type": "object",
    "properties": {
      "event_id": {
        "type": "string",
        "description": "Detection ID of the threat event."
      },
      "source": {
        "type": "string",
        "description": "Event source (default: EMAIL)."
      },
      "page_size": {
        "type": "integer",
        "description": "Results per page (1-100, default 100).",
        "minimum": 1,
        "maximum": 100
      }
    },
    "required": ["event_id"]
  },
  "description": "Retrieve detailed information for a specific Mimecast threat event by its detection ID.",
  "docker_parameters": "get_threat_event_details",
  "name": "Get Threat Event Details",
  "results": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Results",
    "type": "object",
    "properties": {
      "id": { "type": "string", "description": "Detection ID." },
      "sender": { "type": "string", "description": "Sender email address." },
      "recipients": { "type": "array", "items": { "type": "string" }, "description": "Recipient email addresses." },
      "subject": { "type": "string", "description": "Email subject." },
      "threats": { "type": "array", "items": { "type": "object" }, "description": "Detected threats." },
      "attachments": { "type": "array", "items": { "type": "object" }, "description": "Email attachments." },
      "urls": { "type": "array", "items": { "type": "object" }, "description": "URLs found in the email." },
      "timestamp": { "type": "string", "description": "Event timestamp." },
      "status": { "type": "string", "description": "Current event status." }
    }
  },
  "uuid": "a82e278f-17b8-4682-875e-ad1f44976cbf",
  "slug": "get_threat_event_details"
}
